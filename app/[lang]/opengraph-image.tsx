// Route-group shim: file-convention images must live inside each root
// layout group to be attached to its pages; the artwork is shared.
export { default, alt, size, contentType } from "../_og-image";
